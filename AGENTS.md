任何涉及播放、暂停、静音、试听、打轴的改动，动手前必须先读 docs/audio-lifecycle-spec.md，并在 PR 描述里逐条引用你满足了哪些 AUD-x。


## 音频相关改动（强制）

任何触及播放、暂停、静音、试听、打轴、播放列表条目增删的改动，
动手前必须先完整阅读 `docs/audio-lifecycle-spec.md`。

规则：
1. 该 spec 是唯一真相源，**只读不改**。发现矛盾或缺失，停止编码并提问。
2. PR 描述必须包含 `## 满足条款` 小节，逐条列出本次实现的 `AUD-x`。
3. PR 描述必须包含 `## 验收` 小节，给出 AUD-32 中相关场景的
   `/mbm debug session` 实际输出。
4. 不得引入 AUD-29 禁止项清单中的任何一项。
5. 超出任务"只允许修改"文件清单的改动一律不做，先提问。
## 通用判据（profiler 复盘后确立）

1. **状态枚举必须被消费**：新增状态字段/枚举时，同一提交必须给出全部读取点清单。存在但从不被读的状态枚举等于没有状态机。
2. **主线程 Runnable 必须有时间预算**：任何在主线程执行的、由调用方提供的 Runnable（如门控 action），其契约必须写明时间预算；禁止把阻塞工作（音频设备 open/close、线程创建、I/O、同步日志）塞进此类回调。
3. **诊断通道必须与被诊断系统解耦**：探针不得依赖被诊断系统的调度、权限、网络往返或运行状态。若 bug 的表现是"系统卡住/暂停/断连"而探针需要该系统正常运转才能读取，则该探针对此类 bug 恒等于不存在。卡死态下聊天框不可用——必须有键位触发 + 环形缓冲回溯。

4. **一位两义必须拆位**：同一布尔位承担两个以上语义时（如"锚有效"与"需要重锚"），必须拆成独立位，禁止复用。
5. **布尔"假"作触发条件时必须枚举全部置假写入点并逐一确认**：以 !flag 为条件的逻辑，其每个可能使 flag 变 false 的写入点都必须列出并逐一核对语义。
